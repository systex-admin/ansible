- name: "STEP 1 - CONNECT TO F5"
  hosts: 10.241.62.104
  become: yes
  become_user: root
  become_method: sudo
  tasks:
    - name: "PING"
      ping:
      register: message
    - name: "RESULT"
      debug:
        msg: "{{ message }}"  
    - name: Read users from CSV file and return a dictionary
      read_csv:
    - name:  Match 'Li' on the first column, return the second column (0 based index)
      debug: 
        msg: "{{ lookup('csvfile', 'Li file=/root/internat_20200206.csv delimiter=,') }}"
        #path: "/root/internat_20200206.csv"
